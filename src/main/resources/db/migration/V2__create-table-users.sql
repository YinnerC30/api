CREATE TABLE users (
                       id_user INT PRIMARY KEY ,
                       name varchar(50),
                       last_name varchar(50),
                       password varchar(50),
                       is_active bool
);